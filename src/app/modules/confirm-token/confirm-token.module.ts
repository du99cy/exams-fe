import { NgModule } from '@angular/core';
import { ConfirmTokenRoutingModule } from './confirm-token-routing.module';

const MODULES = [ConfirmTokenRoutingModule]

@NgModule({
  imports: [ ...MODULES ],
  declarations: [  ],
})
export class ConfirmTokenModule { }
